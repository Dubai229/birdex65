// Все числа экономики — только здесь. В UI ничего не хардкодим.
// Когда появится бэкенд, этот конфиг будет приходить с сервера.

export const ECONOMY = {
  /** Сколько Coins дают за одно яйцо на рынке. */
  eggSellPrice: 5,
  /** Максимум часов оффлайн-производства. */
  offlineCapHours: 8,
  /** Стартовая вместимость склада яиц. */
  startStorageCapacity: 1000,
  /** Рост стоимости улучшения за уровень. */
  upgradeGrowth: 1.35,
  /** Рост производства за уровень. */
  productionGrowth: 1.18,
  /** Стартовые Coins нового игрока. */
  startCoins: 500,

  energy: {
    max: 1000,
    /** Восстановление энергии в секунду. */
    regenPerSecond: 50 / 135,
    /** Сколько энергии стоит поймать одно яйцо. */
    costPerEgg: 1,
  },

  play: {
    /** Длительность одной сессии Play, секунды. */
    sessionSeconds: 60,
    /** Раз в сколько мс спавнится яйцо. */
    spawnEveryMs: 550,
    /** Сколько мс яйцо падает до низа экрана. */
    fallDurationMs: 2600,
    goldenChance: 0.06,
    normalReward: 1,
    goldenReward: 5,
    /** Окно между ловлями, чтобы комбо не сбросилось, мс. */
    comboWindowMs: 900,
    comboSteps: [1, 2, 3, 5] as readonly number[],
    /** Сколько поймать подряд для следующего шага комбо. */
    comboEvery: 5,
    /** Максимум яиц в секунду (анти-чит лимит сессии). */
    maxEggsPerSecond: 6,
  },

  /** Награда за 7 дней серии (Coins). */
  rewardStreak: [100, 150, 250, 400, 600, 900, 2000] as readonly number[],
  /** Через сколько часов доступна следующая награда. */
  rewardCooldownHours: 20,
  /** Если не заходил дольше — серия сбрасывается. */
  rewardResetHours: 48,
} as const
